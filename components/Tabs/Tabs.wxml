<view class="tabs-container">
  <view class="tabs-header">
    <view class="tabs-title {{ item.isActive ? 'active': ''}}" wx:for="{{ tabs }}" wx:key="id" bindtap="handleTap" data-index="{{index}}">{{ item.title }}</view>
  </view>
  <view class="tabs-content">
    <slot></slot>
  </view>
</view>

